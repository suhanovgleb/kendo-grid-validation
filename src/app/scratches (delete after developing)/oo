

class Uniquie{
    validation();
}

class XXX{
    source,
    changedData,
    schema,
    validators[Uniquie]
}

//valService(items, changedData, schema, validators[]){
valService(XXX){

.forEach(changedData, item,{
    //default validators based on schema
})

//validators[0] = {Uniquie name, ()=>{})

validators['uniqueConstraints'] (items, fieldName);

}